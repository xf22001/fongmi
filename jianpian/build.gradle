plugins {
    alias libs.plugins.android.library
}

android {
    namespace 'com.p2p'

    compileSdk {
        version = release(36) {
            it.minorApiLevel = 1
        }
    }

    defaultConfig {
        minSdk 23
        targetSdk 36
    }
}

dependencies {
    implementation project(':catvod')
}